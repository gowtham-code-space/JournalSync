/**
 * navigation/index.js
 * Barrel for any reusable navigation UI components.
 * Currently a placeholder — add named exports here as nav
 * components (Breadcrumb, Tabs, Pagination, etc.) are built.
 *
 * Example future export:
 *   export { default as Breadcrumb } from './Breadcrumb'
 *   export { default as TabBar }     from './TabBar'
 */

// No navigation components created yet.
export {}
