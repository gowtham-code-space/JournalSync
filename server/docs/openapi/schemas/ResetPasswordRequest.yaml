type: object
required:
  - token
  - password
properties:
  token:
    type: string
    description: Password reset token received in the recovery email.
    example: "password-reset-token-abc987"
  password:
    type: string
    minLength: 8
    description: New password to be set.
    example: "NewStrongPassword123!"
